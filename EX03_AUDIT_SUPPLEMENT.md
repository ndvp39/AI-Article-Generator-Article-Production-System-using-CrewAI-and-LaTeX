# Дополнение к SOFTWARE_PROJECT_GUIDELINES — Аудит-чеклист для מטלה 03 (CrewAI + LaTeX)

> **Назначение.** Этот файл читается **вместе** с `SOFTWARE_PROJECT_GUIDELINES.md`. Он не заменяет гайдлайны, а добавляет приоритеты под конкретное задание 03 (команда агентов на CrewAI пишет статью/книгу → достойный PDF через LaTeX).
>
> **Инструкция аудитору (человек или LLM).** Пройди по проекту пункт за пунктом. Для каждого невыполненного пункта — отметь его цветом приоритета и дай конкретный фикс (файл/строка/что добавить). Сначала прогони блок **«Быстрый скан дилбрейкеров»**: если хоть один из них не выполнен — работу нельзя сдавать как есть.
>
> **Источники в скобках:** `(задание)` = скрин Moodle מטלה 03; `(лекция)` = L06; `(§N)` = раздел SOFTWARE_PROJECT_GUIDELINES.
>
> **Легенда приоритетов:**
> - 🔴 **КРАСНЫЙ** — критично. Прямое требование задания, явно сказанное на лекции, или `MANDATORY` в гайдлайнах. Без этого работа проваливается или нарушает обязательное.
> - 🟠 **ОРАНЖЕВЫЙ** — скорее всего очень важно. Сильные пункты рубрики (`MUST` в гайдлайнах + акценты лекции), которые напрямую поднимают оценку.
> - 🟡 **ЖЁЛТЫЙ** — стоит сделать/поправить. Полировка и доп. баллы; отсутствие не валит, но улучшает.
>
> **Контекст оценки.** Грейдит AI-агент по принципам из `SOFTWARE_PROJECT_GUIDELINES`. Оценка идёт **по «конверту» (техника PDF) и по инженерному процессу**, а не по фактической правильности содержания статьи `(лекция §13)`.

---

## ⚡ Быстрый скан дилбрейкеров (8 пунктов — проверь первыми)

- [ ] PDF: ~15 страниц + титульник + оглавление + главы + колонтитулы `(задание)`
- [ ] PDF содержит **все 4 артефакта**: 1 картинка + 1 Python-график + 1 таблица + 1 формула `(задание)`
- [ ] Минимум одна **глава с BiDi** (иврит↔английский), переходы RTL/LTR не ломаются `(задание)`
- [ ] **Библиография есть, клик по цитате прыгает на источник** (иначе недокомпилили) `(задание, лекция §13.2)`
- [ ] В корне репы есть `README.md` `(§1.1)`; в `docs/` есть `PRD.md`, `PLAN.md`, `TODO.md` `(задание, §1.2)`
- [ ] В репе лежит **папка LaTeX-проекта**, которым собран PDF `(задание)`
- [ ] Имя файла строго `xxxxxxxx-exyy.pdf`, код группы — 8 символов без пробелов `(задание)`
- [ ] **0 секретов/API-ключей в коде**; есть `.env-example` `(§16.4)`

---

## 🔴 КРАСНЫЙ — критично

### PDF: содержание (требования задания)
- [ ] Объём ~15 страниц `(задание §13.1)`
- [ ] Титульник: тема, автор, дата, курс, лектор `(задание)`
- [ ] Оглавление, разбивка на главы, колонтитулы (headers/footers) `(задание)`
- [ ] Минимум **1 картинка** `(задание)`
- [ ] Минимум **1 график, сгенерённый Python-кодом** (не вставленная картинка) `(задание)`
- [ ] Минимум **1 таблица** `(задание)`
- [ ] Минимум **1 математическая формула** `(задание)`
- [ ] Минимум **1 глава с миксом иврит–английский (BiDi)**, корректные переходы LTR↔RTL `(задание)`
- [ ] Библиография в конце, цитаты **кликабельны и связаны** с источниками `(задание)`

### PDF: технический «конверт» (то, что реально проверяют — лекция)
- [ ] Все ссылки/цитаты **прыгают** при клике → значит сделано ~4 компиляции `.tex`+`.bib` `(лекция §13.2)`
- [ ] Формулы «**fancy**», а не плоский текст (BiDi часто ломает формулу в текст) `(лекция §13.2)`
- [ ] **Таблицы не вылезают** за поля страницы `(лекция §13)`

### Репозиторий и документация (MANDATORY)
- [ ] `README.md` в корне — уровень руководства пользователя `(§1.1, §16.1)`
- [ ] `docs/PRD.md` — требования, цели, KPI, критерии приёмки `(§1.2)`
- [ ] `docs/PLAN.md` — архитектура, ADR `(§1.2)`
- [ ] `docs/TODO.md` — задачи со статусами и Definition of Done `(§1.2)`
- [ ] Все три (`PRD/PLAN/TODO`) — **в формате Markdown** `(задание)`
- [ ] Папка LaTeX-проекта присутствует в репе `(задание)`

### Безопасность и упаковка (MANDATORY)
- [ ] **0 API-ключей/секретов** в коде `(§16.4, таблица §17)`
- [ ] `.env-example` с плейсхолдерами `(§16.4)`
- [ ] `.gitignore` актуален (не коммитятся секреты/мусор) `(§16.4)`
- [ ] **Только `uv`** как пакетный менеджер `(§16.4, §17)`
- [ ] Есть `pyproject.toml` и `uv.lock` `(§16.4)`

### Механика сдачи (легко потерять баллы)
- [ ] Имя PDF: `xxxxxxxx-exyy.pdf` (код группы + номер задания) `(задание)`
- [ ] Код группы — **8 символов без пробелов** (пример `uoh-rl07`) `(задание)`
- [ ] **Каждый член группы сдаёт на Moodle отдельно** (время-окно меряют индивидуально) `(задание)`
- [ ] Репа расшарена на `rmisegal@gmail.com` **или публичная** `(задание)`
- [ ] Word-шаблон: **только заполнить поля**, ничего не двигать/не дописывать/не удалять `(задание)`

---

## 🟠 ОРАНЖЕВЫЙ — скорее всего очень важно

### Архитектура (поднимает оценку)
- [ ] **SDK-слой**: вся бизнес-логика доступна через SDK; в CLI/GUI логики нет `(§3.1, §16.2)`
- [ ] **ApiGatekeeper**: все внешние вызовы (LLM-провайдер, Serper/поиск) идут через него `(§4.1, §16.2)`
- [ ] Rate-limits берутся **из конфига**, не хардкод `(§4.2)`
- [ ] При переполнении — **очередь**, а не падение `(§4.3, §17)`
- [ ] Выделенный `PRD_*.md` на ключевой механизм (оркестрация CrewAI и/или LaTeX-пайплайн) `(§1.3)`
- [ ] Версионирование: старт `1.00`, версия у модулей/конфигов/схем `(§17)`

### Качество кода и тесты
- [ ] **TDD**, покрытие **≥85%** (suite падает ниже порога) `(§5.2, §16.3)`
- [ ] `ruff check` — **0 нарушений** `(§6.1, §17)`
- [ ] Каждый файл **≤150 строк кода** (тесты тоже) `(§2.2, §5.1)`
- [ ] OOP без дублирования; общая логика вынесена в base/mixin `(§3.2, §16.2)`
- [ ] Docstrings у каждой функции/класса/модуля; комментарии объясняют **WHY** `(§2.3)`
- [ ] Нет хардкод-значений — всё из конфигов `(§6.2, §17)`

### Прод-принципы из лекции (этот курс именно про это)
- [ ] **Наблюдаемость/логирование** вызовов агентов и токенов — не опция, а обязанность `(лекция §8, §6)`
- [ ] **Анализ стоимости токенов** (по агентам: исследователь/писатель/проверяющий) + стратегия оптимизации `(лекция §12.2, §16.5)`
- [ ] **Human-in-the-Loop / точки контроля** в пайплайне (а не бесконтрольная петля) `(лекция §11.4)`
- [ ] Код-агенты гоняются в **Sandbox/WSL**, если есть исполнение кода `(лекция §5)`

### CrewAI-специфика
- [ ] Чёткое разделение ролей: `Agent` (role/goal/backstory/tools) `(лекция §12)`
- [ ] `Task` = description + **expected_output** `(лекция §12)`
- [ ] Связка через `context=[...]` — выход одного агента = вход следующего `(лекция §12.2)`
- [ ] Корректный `Process` (Sequential для линейного: исследователь→писатель→проверяющий) `(лекция §12.1)`

---

## 🟡 ЖЁЛТЫЙ — стоит сделать/поправить

### Документация и диаграммы
- [ ] В `PLAN.md` — диаграммы **C4** (Context/Container/Component/Code) и UML `(§1.2, §16.1)`
- [ ] **Книга промптов** (documented prompts book) `(§16.1)`
- [ ] Документированы **extension points** `(§16.6)`
- [ ] License & Credits, упоминание соответствия **ISO/IEC 25010** `(§16.6)`

### Эксперименты и визуализация
- [ ] Систематические прогоны с вариацией параметров + анализ чувствительности `(§16.5)`
- [ ] Скриншоты, графики качества, диаграмма архитектуры в README/docs `(§16.5)`

### Прод-полировка из лекции
- [ ] **Red Team / тесты на атаки** (Prompt Injection, Memory Poisoning) — хотя бы упоминание `(лекция §8)`
- [ ] Использование **`SKILL.md`** в CrewAI для инжекта экспертизы агентам `(лекция, Приложение А)`
- [ ] **TikZ** для блок-схем в PDF (красивее, чем картинка) `(лекция §13.2)`
- [ ] Модульность провайдера: смена модели через конфиг, не hard-code `(лекция §9, §11.2)`
- [ ] Если поток ветвится/циклит — рассмотреть `LangGraph`/`Hierarchical` (для этой задачи Sequential обычно достаточно) `(лекция §3, §12.1)`

### Гигиена и привычки
- [ ] Чистая история Git, осмысленные коммиты `(§16.6)`
- [ ] ⚠️ Проверить `.tex`/Markdown/промпты на **скрытые HTML-инструкции** (привычка против ловушек лектора; здесь грейдит AI, но лучше перебдеть)
- [ ] LuaLaTeX как компилятор (лучший иврит); сначала Markdown, конверт в `.tex` только на финале `(лекция §13.2)`

---

*Версия 1.00 — дополнение к SOFTWARE_PROJECT_GUIDELINES v3.00 (Dr. Yoram Segal). Составлено для аудита מטלה 03.*
